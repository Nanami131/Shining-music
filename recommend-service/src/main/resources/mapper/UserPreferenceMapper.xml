<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.L2.recommend.infrastructure.UserPreferenceMapper">

    <select id="selectByUserId" resultType="org.L2.recommend.domain.model.UserPreference">
        SELECT user_id, vector_json, play_count, snapshot_at
        FROM user_preferences
        WHERE user_id = #{userId}
    </select>

    <insert id="insertOrUpdate">
        INSERT INTO user_preferences (user_id, vector_json, play_count, snapshot_at)
        VALUES (#{userId}, #{vectorJson}, #{playCount}, NOW())
        ON DUPLICATE KEY UPDATE
            vector_json = VALUES(vector_json),
            play_count  = VALUES(play_count),
            snapshot_at = NOW()
    </insert>

</mapper>
