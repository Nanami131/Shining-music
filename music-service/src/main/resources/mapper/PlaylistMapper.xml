<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.L2.music.infrastructure.PlaylistMapper">
    <resultMap id="PlaylistResultMap" type="org.L2.music.domain.model.Playlist">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="visibility" column="visibility"/>
        <result property="coverUrl" column="cover_url"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="PlaylistResultMap">
        SELECT * FROM playlists WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="org.L2.music.domain.model.Playlist" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO playlists (name, description, user_id, type, visibility, cover_url, created_at, updated_at)
        VALUES (#{name}, #{description}, #{userId}, #{type}, #{visibility}, #{coverUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="org.L2.music.domain.model.Playlist">
        UPDATE playlists
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="visibility != null">visibility = #{visibility},</if>
            <if test="coverUrl != null and coverUrl != ''">cover_url = #{coverUrl},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM playlists WHERE id = #{id}
    </delete>

    <select id="query" resultMap="PlaylistResultMap">
        SELECT * FROM playlists
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="name != null and name != ''">
                AND name = #{name}
            </if>
            <if test="description != null and description != ''">
                AND description = #{description}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="visibility != null">
                AND visibility = #{visibility}
            </if>
            <if test="coverUrl != null and coverUrl != ''">
                AND cover_url = #{coverUrl}
            </if>
            <if test="createdAt != null">
                AND created_at >= #{createdAt}
            </if>
            <if test="updatedAt != null">
                AND updated_at >= #{updatedAt}
            </if>
        </where>
        ORDER BY id
    </select>

    <sql id="visiblePlaylistFilter">
        type IN (1, 2) AND (user_id = -1 OR visibility = 0
        <if test="includeOwn and userId != null"> OR user_id = #{userId}</if>
        )
        <if test="!includeOwn">AND (visibility IS NULL OR visibility != 1)</if>
    </sql>

    <select id="selectVisiblePage" resultMap="PlaylistResultMap">
        SELECT * FROM playlists WHERE <include refid="visiblePlaylistFilter" />
        <if test="search != null and search != ''">
            AND (name LIKE CONCAT('%', #{search}, '%') OR description LIKE CONCAT('%', #{search}, '%'))
        </if>
        ORDER BY id
        <if test="size != null and size > 0">LIMIT #{size} OFFSET #{offset}</if>
    </select>

    <select id="countVisible" resultType="java.lang.Long">
        SELECT COUNT(*) FROM playlists WHERE <include refid="visiblePlaylistFilter" />
        <if test="search != null and search != ''">
            AND (name LIKE CONCAT('%', #{search}, '%') OR description LIKE CONCAT('%', #{search}, '%'))
        </if>
    </select>

    <select id="selectPublicOwnerPage" resultMap="PlaylistResultMap">
        SELECT * FROM playlists WHERE user_id = #{creatorId} AND type IN (1, 2) AND visibility = 0
        ORDER BY id
        <if test="size != null and size > 0">LIMIT #{size} OFFSET #{offset}</if>
    </select>

    <select id="countPublicOwner" resultType="java.lang.Long">
        SELECT COUNT(*) FROM playlists
        WHERE user_id = #{creatorId} AND type IN (1, 2) AND visibility = 0
    </select>

    <select id="selectOwnPage" resultMap="PlaylistResultMap">
        SELECT * FROM playlists WHERE user_id = #{userId} AND type IN (1, 2)
        ORDER BY id LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countOwn" resultType="java.lang.Long">
        SELECT COUNT(*) FROM playlists WHERE user_id = #{userId} AND type IN (1, 2)
    </select>
</mapper>
