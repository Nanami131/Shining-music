<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.L2.community.infrastructure.mapper.CommentMapper">
    <resultMap id="CommentResultMap" type="org.L2.community.domain.model.Comment">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="likes" column="likes"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="CommentResultMap">
        SELECT * FROM comments WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="org.L2.community.domain.model.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (user_id, content, target_type, target_id, parent_comment_id, likes, status, created_at, updated_at)
        VALUES (#{userId}, #{content}, #{targetType}, #{targetId}, #{parentCommentId}, #{likes}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="org.L2.community.domain.model.Comment">
        UPDATE comments
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="content != null and content != ''">content = #{content},</if>
            <if test="targetType != null and targetType != ''">target_type = #{targetType},</if>
            <if test="targetId != null">target_id = #{targetId},</if>
            <if test="parentCommentId != null">parent_comment_id = #{parentCommentId},</if>
            <if test="likes != null">likes = #{likes},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM comments WHERE id = #{id}
    </delete>

    <select id="query" resultMap="CommentResultMap">
        SELECT * FROM comments
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="content != null">
                AND content LIKE CONCAT('%', #{content}, '%')
            </if>
            <if test="targetType != null">
                AND target_type = #{targetType}
            </if>
            <if test="targetId != null">
                AND target_id = #{targetId}
            </if>
            <if test="parentCommentId != null">
                AND parent_comment_id = #{parentCommentId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="createdAt != null">
                AND created_at >= #{createdAt}
            </if>
        </where>
    </select>
</mapper>